set -g default-terminal "screen-256color"

# Remap prefix to screen (Ctrl-a)
set -g prefix C-a
bind C-a send-prefix
unbind C-b

# Quality of life
set -g history-limit 10000
set -g allow-rename off

# Search mod Vi (default is emacs)
set-window-option -g mode-keys vi
